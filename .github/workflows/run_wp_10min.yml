name: Run WP V41 Fixed 14:00 Session

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - scripts/trigger_wp_run.txt

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: wp-v3-output-writer-${{ github.ref }}
  cancel-in-progress: false

jobs:
  tail-session:
    runs-on: ubuntu-latest
    timeout-minutes: 80
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.lock
      - name: Install dependencies
        run: pip install -r requirements.lock
      - name: Restore compact V41 runtime references
        uses: actions/cache@v4
        with:
          path: |
            data/v3/cache/trade_cal
            data/v3/cache/daily
            data/v3/cache/daily_basic
            data/v3/cache/adj_factor
            data/v3/cache/stk_limit
            data/v3/cache/stock_basic
            data/v3/cache/index_member_all
          key: wp-v41-runtime-ref-v1-${{ runner.os }}-${{ hashFiles('config/wp_v3.yml', 'src/wp/v3/history.py', 'src/wp/v3/live_data.py') }}-${{ github.run_id }}
          restore-keys: |
            wp-v41-runtime-ref-v1-${{ runner.os }}-${{ hashFiles('config/wp_v3.yml', 'src/wp/v3/history.py', 'src/wp/v3/live_data.py') }}-
            wp-v41-runtime-ref-v1-${{ runner.os }}-
      - name: Verify exact V41 runtime package
        run: python scripts/check_wp_runtime_readiness.py
        env:
          PYTHONPATH: src
      - name: Run exact 14:00 session or labeled same-day recovery
        run: python scripts/run_wp_session.py
        env:
          PYTHONPATH: src
          WP_MODE: live
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          WP_RUN_MODE: auto
          WP_SCHEDULE_GRACE_SECONDS: "120"
          WP_MAX_CAPTURE_LATENESS_SECONDS: "45"
          LOKY_MAX_CPU_COUNT: "2"
      - name: Prune and report compact runtime references
        run: |
          python scripts/prune_wp_runtime_cache.py
          du -ch \
            data/v3/cache/trade_cal \
            data/v3/cache/daily \
            data/v3/cache/daily_basic \
            data/v3/cache/adj_factor \
            data/v3/cache/stk_limit \
            data/v3/cache/stock_basic \
            data/v3/cache/index_member_all \
            2>/dev/null | tail -n 1 || true
