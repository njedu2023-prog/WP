name: Probe WP V25 Positioning Data

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - scripts/trigger_wp_v25_data_probe.txt

permissions:
  contents: read

concurrency:
  group: wp-v25-positioning-data-probe-${{ github.ref }}
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.lock
      - name: Install locked dependencies
        run: pip install -r requirements.lock
      - name: Verify outcome-blind V25 probe contracts
        run: >-
          python -m pytest
          tests/test_v25_positioning_data_probe.py
          -q
        env:
          PYTHONPATH: src:scripts
      - name: Probe independent positioning data
        run: python -u scripts/probe_wp_v25_positioning_data.py
        env:
          PYTHONPATH: src
          PYTHONUNBUFFERED: "1"
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          WP_V25_PROBE_OUTPUT: artifacts/wp_v25_data_probe
      - name: Upload immutable V25 probe evidence
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: wp-v25-positioning-data-probe-${{ github.run_id }}
          path: artifacts/wp_v25_data_probe
          retention-days: 90
          if-no-files-found: warn

