# WP V5 - 尾盘 T+1 正净收益候选系统

WP 只优化一个可检验目标：

> 最大化 A 股主板股票在 T 日 14:20-14:50 可成交买入后，按固定 T+1
> 收盘卖出并扣除既定往返成本后，净收益大于 0 的概率。

系统发布全部通过固定门槛的候选，由人工决定是否买入以及买哪一支。零候选是
正常结果，模型不得为了生成名单放宽门槛。

## 设计原则

- 标签就是固定交易合同的 `net_return_pct > 0`，不是涨停、排名或未来最高价。
- 历史训练和实时推理共享同一套 5 分钟因果特征代码。
- 候选由绝对盈利概率、同槽横截面排序、收益均值和下行分位共同决定。
- 概率校准期与政策证据期按时间前后分开，测试集不参与门槛或模型选择。
- 每支股票第一次通过即锁定信号时间、信号价、模型和政策指纹。
- 所有 14:20-14:50 出现过的候选逐票验证；模型候选不等于用户实际成交。
- 三年滚动样本外回测通过后，仍必须完成至少 150 个未来交易日的影子运行。

## 固定交易合同

| 项目 | 合同 |
| --- | --- |
| 信号时点 | 14:20、14:25、14:30、14:35、14:40、14:45、14:50 |
| 股票范围 | A 股主板 |
| 参考订单 | 100,000 元，且不超过当前 5 分钟成交额的 1% |
| 入场价 | 首次信号价加 10bp 滑点 |
| 费用及退出影响 | 25bp |
| 基准全成本 | 35bp |
| 压力全成本 | 50bp |
| 退出 | 下一 A 股交易日收盘 |
| 无法退出 | 停牌或 T+1 收盘钉在跌停价时计为失败；缺价采用预先声明的 -10% 惩罚 |
| 截止 | 14:50 后禁止新增，14:55 冻结，15:00 清除可买展示 |

完整定义见 [STRATEGY_CONTRACT.md](docs/STRATEGY_CONTRACT.md)。

## 模型

252 日和 504 日两个时间尺度分别训练正净收益、横截面领先、严重损失、市场
状态四个概率模型，一个 LambdaRank 横截面排序模型，以及净收益 L1 与 10%
下行分位模型。四类概率均使用独立 42 日窗口进行 Beta calibration。

最终选择分综合正收益概率、横截面领先概率、LambdaRank、期望收益、下行分位
和严重损失安全分位。资格门槛只允许由当前测试折之前的 84 日样本外设计期搜索，
并由随后 42 日样本外确认期授权；任一证据不足则该折输出 `NO_SIGNAL`。

特征覆盖：

- 14:00 至当前槽的趋势、波动、回撤、反弹、价格效率和成交额结构；
- 当前 5 分钟实体、振幅和上下影线；
- 严格滞后的日频动量、量价、波动、回撤、换手、估值和规模；
- 当时市场与申万一级行业的收益、广度、离散度和相对强弱；
- 同一交易日、同一信号槽内的横截面分位。

算法、特征和限制见 [WP_V3_MODEL_CARD.md](docs/WP_V3_MODEL_CARD.md)，三年
验证方法见 [WP_V3_BACKTEST_PROTOCOL.md](docs/WP_V3_BACKTEST_PROTOCOL.md)。
文件路径保留 `v3` 名称是为了兼容既有 Pages、账本和自动化接口，不表示仍在
运行旧模型。

## 三条链路

1. `Run WP V5 Tail Session`：13:56 启动持续进程，14:00-14:15 暖机，
   14:20-14:50 每 5 分钟推理，14:55 冻结，15:00 清场。
2. `Validate WP Close`：15:08-16:10 获取到期 T+1 收盘真值并逐票结算。
3. `Research WP V5 Full Three-Year OOS Model`：构建约五年 point-in-time
   因果面板，最后连续三年作为正式样本外审计，并登记研究、影子观察或影子模型。

上游仓库只用于归档，不阻塞 WP 的实时推理。

## 验证

```bash
python -m venv .venv
.venv/bin/pip install -r requirements.txt
PYTHONPATH=src:. .venv/bin/pytest -q
```

三年研究需要 `TUSHARE_TOKEN`：

```bash
PYTHONPATH=src .venv/bin/python scripts/build_wp_v3_history.py
PYTHONPATH=src .venv/bin/python scripts/run_wp_v3_research.py
```

GitHub Pages：

```text
https://njedu2023-prog.github.io/WP/outputs/html_reports/latest.html
```

系统只提供人工决策支持，不连接券商、不自动下单，也不保证收益。
